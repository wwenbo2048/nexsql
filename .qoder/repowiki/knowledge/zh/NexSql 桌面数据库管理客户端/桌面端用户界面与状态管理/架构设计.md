- 采用 React + TypeScript 构建组件化视图，入口为 `main.tsx` 挂载 `App.tsx`。
- 使用 Zustand 实现全局状态管理，划分为 `connection`（连接配置）、`tab`（标签页逻辑）和 `ui`（界面状态）三个独立 Store。
- 界面布局采用经典的“侧边栏-主内容区”结构，侧边栏展示连接树，主区域根据标签类型动态渲染查询编辑器或数据表格。
- 通过 `window.api` 桥接对象与主进程通信，实现配置持久化等底层操作。