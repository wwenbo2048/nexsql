- 使用 Zustand 的 `create` API 定义 Store，并在组件中通过选择器模式（如 `useStore((s) => s.prop)`）订阅状态以优化渲染性能。
- 功能型组件以默认导出形式定义（`export default function ComponentName`），并存放于 `components/` 目录下。
- 通过 `@stores/` 和 `@components/` 等路径别名引用内部模块，保持导入路径简洁且与物理目录结构对应。