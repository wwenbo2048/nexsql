- 入口层：`index.ts` 作为主进程入口，管理 `BrowserWindow` 生命周期并注册全局事件监听（如 `before-quit`）。
- 通信层：`ipc.ts` 封装 `ipcMain` 处理器，统一处理来自渲染进程的请求，并通过 `electron-store` 持久化连接配置。
- 服务层：`services/db.ts` 基于 `mysql2` 实现连接池管理与元数据查询，提供统一的数据库操作接口。
- 依赖方向：入口依赖通信层，通信层依赖服务层与工具层（`uuid.ts`），形成单向依赖链。